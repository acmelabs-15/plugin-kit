---
name: chart-maker
description: Build a rendered chart from a spreadsheet and place it in a slide deck. Use when a chart image is the deliverable.
---

# Chart maker

## Overview

Boilerplate heading above, deliberately, so the extractor has something to drop.

## Pick a chart form

Choose the form from the shape of the data.

## Rendering the chart

Rasterize at print resolution.

## Step 1: Read the sheet

A procedure step, not a capability.

## Placing it in a deck

Do not use this to clean up the source spreadsheet — that is a spreadsheet job.

```markdown
## Not a real heading
This lives inside a fenced block and belongs to the example, not to the artifact.
```
